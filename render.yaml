services:
  - type: web
    name: ai-stack-doctor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn dashboard_server:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.0
